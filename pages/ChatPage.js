import React from 'react';
import { StyleSheet, Text, View, Image, TouchableOpacity, ScrollView } from 'react-native';

const ChatPage = () => {
    return (
        <Text>Chatting function will be mounted.</Text>
    );

}

export default ChatPage;